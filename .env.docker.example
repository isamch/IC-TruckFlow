# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=admin123
MONGO_INITDB_DATABASE=truckflow

# Backend Configuration
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://admin:admin123@mongodb:27017/truckflow?authSource=admin

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_ACCESS_SECRET=change-this-secret-key
JWT_REFRESH_SECRET=change-this-secret-key
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# HMAC Secret (CHANGE THIS IN PRODUCTION!)
HMAC_VERIFICATION_CODE_SECRET=change-this-secret-key

# Frontend Configuration
VITE_API_URL=http://localhost:5000/api/v1
